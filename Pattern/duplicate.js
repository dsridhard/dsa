// find the duplicate values from array

const duplicate = () => {


    for (let i = 0; i < arr.length; i++) {
        console.log(arr[i])
    }
}
let arr = [2, 3, 1, 2, 3]
arr.sort((a, b) => a - b)
console.log(arr)
// Not Completed 